# LiveKit Voice Agent Dependencies

# Core LiveKit Agents Framework
livekit-agents[openai,assemblyai,silero]>=0.8.0
livekit>=0.10.0

# OpenTelemetry - pin compatible versions for livekit-agents
opentelemetry-api>=1.20.0,<1.30.0
opentelemetry-sdk>=1.20.0,<1.40.0
opentelemetry-exporter-otlp>=1.20.0,<1.40.0

# Optional: Additional TTS providers
# livekit-plugins-elevenlabs  # For ElevenLabs TTS
# livekit-plugins-deepgram    # For Deepgram STT

# Noise cancellation (optional but recommended)
livekit-plugins-noise-cancellation

# Environment management
python-dotenv

# Resilience and error handling
tenacity>=8.2.0  # Retry logic with exponential backoff
circuitbreaker>=1.4.0  # Circuit breaker pattern

# Configuration validation
pydantic>=2.0.0  # Configuration validation

# Optional: Session persistence
redis>=5.0.0  # Redis for session storage (optional)

# Optional: Metrics and monitoring
prometheus-client>=0.19.0  # Prometheus metrics (optional)

# Additional providers (optional)
# deepgram-sdk>=3.0.0  # Deepgram STT
# elevenlabs>=0.2.0  # ElevenLabs TTS

# Note: The following dependencies are already in the main project requirements.txt:
# - loguru (for logging)
# - openai (for OpenAI STT/TTS)
# - All RAG pipeline dependencies from Module4_NiruAPI

