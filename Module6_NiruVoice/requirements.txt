# Voice Module Dependencies (Refactored - No LiveKit)

# VibeVoice TTS Dependencies
torch>=2.0.0
transformers==4.51.3
accelerate==1.6.0
diffusers
scipy
numpy
librosa

# Optional: Flash Attention for faster inference (CUDA only)
# flash-attn  # Uncomment if you have CUDA

# Environment management
python-dotenv

# Logging
loguru

# Note: The following dependencies are already in the main project requirements.txt:
# - FastAPI (for API endpoints)
# - pydantic (for request/response models)
# - All RAG pipeline dependencies from Module4_NiruAPI
